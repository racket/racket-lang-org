- the spacing at the top of the tabs is inconsistent

- figure out some better colorschemes

- fix images
  - various ones are stretched incorrectly
  - in the lop tab set, the images (last four tabs)
    need to shrink when the window size gets
    small (the same way the text shrinks in that case)
